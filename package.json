{
  "name": "liangliangqiangqiang",
  "version": "0.2.0-beta.1",
  "type": "module",
  "private": true,
  "description": "踉踉跄跄——一只努力跟上每个任务进度的三花猫桌面宠物",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "validate": "npm run check && npm test",
    "preview": "python -m http.server 4173 -d src",
    "tauri": "tauri",
    "android:init": "tauri android init",
    "android:build": "tauri android build --apk --debug --target aarch64-linux-android"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.8.0"
  }
}
